<!-- 한국어 가이드 · English: GUIDE.en.md · (이 문서와 GUIDE.ko.pdf 내용은 동일합니다) -->

# O-Brain 왕초보 완전 가이드 🧠

> **컴퓨터·AI·프로그램이 처음이어도 그대로 따라 하면 됩니다.** 어려운 말은 ( ) 안에 쉽게 풀어 썼어요.

---

## 0. O-Brain이 뭔가요? (1분 설명)

AI(클로드코드)는 똑똑하지만, **대화창을 닫으면 방금 한 이야기를 다 잊어버립니다.** 마치 *건망증이 심한 친구* 같아요.

**O-Brain은 그 친구 옆에 두는 "자동 메모 노트"입니다.**
- 대화 중 중요한 **결정·약속·금지**를 알아서 적어두고,
- 다음에 다시 알려주고(대화 중 AI가 **관계·변천까지 찾아** 활용),
- 쌓인 기억을 **그래프(지도)·개요(한눈 요약)·목록·시간순**으로 보여줍니다.
- **어느 프로젝트 기억인지 자동으로 분류**해요(그 세션에서 실제로 만진 프로젝트 기준).
- 새 기억은 화면에 **알아서 반영**돼요(자동 새로고침).
- **100% 내 컴퓨터 안에서만** 동작합니다(인터넷으로 안 나감).

> 용어: **클로드코드(Claude Code)** = 터미널에서 쓰는 AI 도구. **플러그인** = 클로드코드에 끼우는 추가 기능. **로컬** = 내 컴퓨터 안.

---

## 1. 빠른 시작 (3분 요약)

급하면 이것만 따라 하세요. 자세한 설명은 3장부터.

1. **Node.js 설치** (없을 때만) → [nodejs.org](https://nodejs.org) 의 **LTS** 버전 → 설치 후 **창을 새로** 엽니다.
2. **클로드코드 입력칸**에 두 줄을 차례로 칩니다:
   ```
   /plugin marketplace add D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\plugin
   /plugin install o-brain@o-brain-local
   ```
3. **클로드코드를 새로 시작**합니다. → 설치 끝.
4. 평소처럼 대화하다 또렷한 **결정 문장**을 말합니다. 예) `이 프로젝트 포트는 7740으로 정하자`
5. 대화를 **끝냅니다**(입력칸에 `/clear`). → 이 순간 자동 저장.
6. 화면으로 보려면 클로드코드에 `/o-brain:open` → 브라우저에 내 기억 지도가 뜹니다.

---

## 2. 사전 준비물 · 필요 프로그램 · 다운로드

| 무엇 | 꼭 필요? | 어디서 / 어떻게 |
|------|:---:|------|
| **Windows PC** | 필수 | 이 가이드는 Windows 기준입니다. |
| **Node.js (LTS)** | 필수 | [nodejs.org](https://nodejs.org) → 큰 초록 **LTS** 버튼 → 다운로드 → 계속 "다음" → 설치. ( "프로그램을 실행하는 엔진"이에요.) |
| **클로드코드(Claude Code)** | 필수 | 이미 쓰고 계신 그 도구입니다. |
| **인터넷** | 설치할 때만 | 첫 설치·첫 실행(작은 AI 모델 1회 다운로드) 때만. 그 뒤엔 인터넷 없이도 동작. |
| **웹 브라우저** | 필수 | 크롬·엣지 등 아무거나(화면 보기용). |
| **API 키·결제** | **불필요** | O-Brain은 키·비용 **0**. 100% 무료·로컬입니다. |

> **다운로드 방법(O-Brain 자체):** 이미 `D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\` 폴더에 들어 있습니다. 따로 받을 게 없어요.

---

## 3. 설치 (딱 2단계, 그림처럼 따라 하기)

> 아래 두 줄은 **클로드코드 입력칸**(프롬프트)에 칩니다. 윈도우 탐색기가 아니라 **클로드코드 안**입니다.

**① 목록 등록** — 플러그인이 있는 폴더를 알려줍니다.
```
/plugin marketplace add D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\plugin
```

**② 설치** — 그 목록에서 O-Brain을 깝니다.
```
/plugin install o-brain@o-brain-local
```

**③ 클로드코드 재시작** → 설치 완료.

> - `o-brain` = 플러그인 이름, `o-brain-local` = 목록(마켓플레이스) 이름.
> - 한 번에 `/plugin install <폴더경로>` 로 하면 **"Marketplace not found"** 오류가 납니다. 꼭 **2단계**로 하세요.
> - "git 저장소가 아니다"라고 막히면, 그 `plugin` 폴더에서 `git init && git add -A && git commit -m init` 후 다시 ①.

---

## 4. 잘 깔렸는지 확인

클로드코드 입력칸에:
- `/o-brain:status` → 쌓인 기억 수·최근 기억·상태를 한국어로 보여줍니다.
- `/o-brain:selftest` → 두뇌(저장·보안가림·검색)를 한 방에 점검. **✅✅✅** 면 정상입니다.

---

## 5. 사용법 & 작동 원리 (제일 중요 — 아주 쉬움)

**작동 원리 한 줄:** *대화를 끝낼 때, O-Brain이 그 대화를 훑어 "결정/약속/금지" 문장만 골라 자동 저장합니다.*

1. **평소처럼 대화**하세요.
2. 기억시킬 내용은 **또렷한 결정 문장**으로 말합니다.
   - 좋은 예: `이 프로젝트 테스트 포트는 7740으로 정하자` / `API 키는 절대 커밋 금지`
   - 안 잡히는 것: 잡담, `cd 폴더` 같은 단순 명령. (또렷한 "정하자/쓰자/금지" 류만 골라 적어요.)
3. 그 대화를 **끝냅니다**: 입력칸에 `/clear` (또는 창 닫기). → **이 순간 자동 저장.**
4. 확인: `/o-brain:status` → 기억 수가 늘고 그 문장이 보이면 성공.

> **보안 자동 처리:** 비밀번호·API키(sk-… 등)는 **저장되기 전에 자동으로 가려집니다**(`[REDACTED]`). 단, "100% 안전"을 보장하진 않으니 늘 조심하세요.

---

## 6. 화면(대시보드) 사용법

**여는 법:** 클로드코드에 `/o-brain:open` (서버가 꺼져 있어도 자동으로 켜고 브라우저를 엽니다).
주소: `http://127.0.0.1:7740/` (이 주소를 즐겨찾기 해두면 클릭 한 번으로 다시 열려요.)

**네 가지 보기 (위쪽 탭 — 첫 화면은 그래프):**
- **그래프** — 기억을 점과 선으로 그린 지도(2D/3D 전환). **여는 순간 바로 이 화면**이에요.
- **개요** — 한눈 요약: 전체 기억 수·최근 7일·**유형/주제/프로젝트별 분포**·가장 많이 연결된 기억·자주 본 기억. "**연결 없는 기억**"을 누르면 그래프에서 외톨이 기억만 모아 봐요.
- **목록** — 기억을 카드로. 유형 배지·중요도(★)·날짜·조회수. (많아지면 아래 **"더 보기"** 로 계속 불러옴.)
- **타임라인** — 시간순.

> **빠른 검색·이동:** 어디서든 **⌘K(맥) / Ctrl+K(윈도우)** 를 누르면 작은 검색창이 떠서 *명령(보기 전환·테마·내보내기)·기억* 을 한 번에 찾아 바로 이동할 수 있어요.

**그래프에서 할 수 있는 것:**
- 점에 **마우스를 올리면** 연결된 기억이 밝게 강조.
- 점을 **클릭하면** 오른쪽에 상세가 열림.
- **검색** — 위 검색창(`/` 키를 누르면 바로 이동). 그래프에선 맞는 점이 밝아짐.
- **"분류색" 버튼** — 점 색을 *유형 ↔ 주제(보안·UI·배포 등 분류)* 로 전환.
- **"전체 보기" / 중심 보기** — 상세창의 **◎(표적) 버튼**을 누르면 *그 기억과 직접 연결된 것만* 크게, "전체 보기"로 복귀.
- **자주 열어본 기억일수록 점이 더 밝게** 빛납니다(조회수).
- **확대/축소** — 오른쪽 아래 ＋ / − / 맞춤 버튼.
- **기억이 많아지면** 그래프는 *가장 중요한 것 위주*로 보여주고 "**전체 N개 중 상위 M개**"라고 알려줘요(느려지지 않게).

**상세창(기억 클릭 시 오른쪽):**
- 내용·유형·분류·중요도·출처·신뢰도·날짜·조회수.
- **복사** / **편집(✏️ 내용·유형·중요도 수정)** / **삭제**(되돌릴 수 없음, 단 서버 시작 시 백업본 있음) / **◎ 중심 보기**.
  - *편집해도 비밀번호·키 같은 민감정보는 저장 시 다시 자동으로 가려집니다.*
- **"이 기억의 관계"** — 다른 기억과 **번복/뒷받침/영향/충돌**로 직접 연결(잘못 누르면 "되돌리기"로 복구).

**위쪽 도구:** 검색 · 유형/분류 칩 · **중요도·기간·프로젝트 필터**(최근 활동순) · 정렬(최신/오래된/중요도) · **새로고침(↻)** · **밝게/어둡게 테마** · **내보내기(JSON/마크다운)**.

> **자동 새로고침:** 새 기억이 생기면 화면이 **알아서 반영**돼요(탭으로 돌아오거나 잠시 기다리면). 급하면 오른쪽 위 **새로고침(↻) 버튼**을 누르면 바로 갱신됩니다.
>
> **프로젝트 자동 분류:** 기억은 **그 세션에서 실제로 편집한 파일이 속한 프로젝트**로 자동 분류돼요. 클로드코드를 어디서 켰든, *가장 많이 만진 프로젝트*로 들어갑니다. (다음 `/clear`부터 적용 — 옛 기억은 예전 분류 그대로.)

---

## 7. 명령어 한눈에 (복붙용)

| 무엇 | 어디에 | 명령 |
|------|------|------|
| 설치 ① 목록 등록 | 클로드코드 | `/plugin marketplace add D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\plugin` |
| 설치 ② 설치 | 클로드코드 | `/plugin install o-brain@o-brain-local` |
| 상태 보기 | 클로드코드 | `/o-brain:status` |
| 자가 점검 | 클로드코드 | `/o-brain:selftest` |
| 백업 | 클로드코드 | `/o-brain:backup` |
| 화면 띄우기(자동·추천) | 클로드코드 | `/o-brain:open` |
| 화면(수동) | 터미널 | `cd D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\app` → `npm start` |
| 화면 주소 | 브라우저 | `http://127.0.0.1:7740/` |
| 백업(수동) | 터미널 | `cd ...\app` → `npm run backup` |

---

## 8. 워크플로우 (하루 흐름 예시)

1. **작업 시작** — 클로드코드를 켜면, 지난 기억 일부가 자동으로 AI에 참고됩니다(되읽기).
2. **작업 중** — 중요한 결정이 나오면 또렷이 말합니다(자동 저장 대상).
3. **작업 끝** — `/clear` 로 세션 종료 → 그 대화의 결정들이 저장됨.
4. **가끔** — `/o-brain:open` 으로 화면을 열어 기억을 둘러보고, 관계를 잇거나 필요 없는 건 삭제.
5. **주기적** — `/o-brain:backup` 으로 백업(서버 켤 때도 자동 백업됨).

---

## 9. 파일 · 데이터 · 문서 위치 (절대경로)

| 무엇 | 위치 |
|------|------|
| 프로젝트 폴더 | `D:\AI_Dev_Work\2026y\26y_06m_21d_O-Brain\` |
| 로컬앱(코드) | `...\app\` |
| 플러그인 | `...\plugin\` |
| **기억 DB(소중함)** | `...\app\data\obrain.db` |
| 백업 폴더 | `...\app\data\backup\` (최신 7개 보관) |
| 설정 파일 | `...\app\.env.local` (예시: `.env.local.example`) |
| 사용설명서(README) | `...\README.md`(한국어) · `...\README.en.md`(영어) |
| 이 가이드 | `...\GUIDE.ko.md` · `...\GUIDE.en.md` (+ 각 `.pdf`) |
| 설계 문서(개발용) | `...\.PRD\` (숨김 폴더, 13개 문서) |
| 구현 브리핑 | `...\AGENTS.md` |

**설정(.env.local) 주요 값:** *(대부분 기본값으로 충분 — 바꿀 때만 `.env.local.example`을 복사해 `.env.local`로)*
- `OBRAIN_PORT` — 화면 주소 포트(기본 7740). 충돌 시 바꾸세요.
- `OBRAIN_DATA_DIR` — 기억을 저장할 폴더(기본 `app\data`).
- `OBRAIN_EXTRACT_MODE` — 추출 방식(기본 `rule`=규칙 기반·무료·로컬). `ai`(Haiku) 모드는 다음 단계 — 켜질 때만 `ANTHROPIC_API_KEY` 필요.
- `OBRAIN_VEC_GATE` — 검색 민감도(기본 0.92, 작을수록 엄격).

---

## 10. 백업 & 복원 (소중한 기억 보호)

- **백업:** `/o-brain:backup` (또는 `npm run backup`). 서버 켤 때도 자동 1회.
  - 위치: `...\app\data\backup\` (최신 7개 자동 보관)
- **복원:** 문제가 생기면 `backup\` 안의 가장 최근 `obrain-....db` 파일을 `app\data\obrain.db` 로 **복사해 덮어쓰기**. (먼저 현재 파일을 다른 곳에 복사해 두세요.)
- ⚠️ 데이터는 위 폴더에만 있습니다 — **클라우드 동기화 폴더에 두지 마세요**(충돌·유출 위험).

---

## 11. 문제 · 오류 대처 (실제 겪은 것 위주)

| 증상 | 원인 | 해결 |
|------|------|------|
| `Marketplace not found` | 한 번에 설치 시도 | **2단계**(목록 등록 → 설치) 순서대로 |
| 기억이 안 쌓임 | 결정 문장 없음 / 세션 안 끝냄 | 또렷한 결정 문장 말하고 **`/clear`** |
| 화면(주소)이 안 열림 | 서버 안 켜짐 / 포트 충돌 | `/o-brain:open` 다시, 또는 `.env.local`에서 `OBRAIN_PORT` 변경 |
| `'node'은(는) 명령이 아닙니다` | Node.js 미설치 | nodejs.org에서 LTS 설치 → **새 창** |
| 3D 그래프가 안 보임 | 그래픽 미지원·저사양 | **2D**로 전환 |
| 새 기억·프로젝트가 화면에 안 보임 | 화면이 옛 상태 | **새로고침(↻) 버튼** 누르기(또는 잠시 후 자동 반영) |
| 프로젝트가 한 개로 합쳐져 보임 | 옛 기억은 예전 방식(시작 폴더)으로 저장됨 | **다음 `/clear`부터** 실제 작업한 프로젝트로 자동 분류(옛 기억은 그대로) |
| 검색이 엉뚱/안 나옴 | 1글자거나 너무 모호 | 2글자 이상, 또렷한 단어로 |
| git 저장소 아니라고 설치 거부 | 로컬 폴더 | `plugin` 폴더에서 `git init && git add -A && git commit -m init` |
| 새 PC로 옮겼더니 안 됨 | 경로가 이 PC에 맞춰져 있음 | 같은 경로로 두거나, 개발자에게 경로 조정 요청(현재 1인·이 PC용) |

> 막히면 **그 화면(또는 오류 메시지)을 그대로** 알려주세요. 함께 고칩니다.

---

## 12. 안전 · 개인정보

- **100% 내 컴퓨터 안**에서만 동작. 기억은 인터넷으로 나가지 않습니다.
- 화면 서버는 **127.0.0.1(내 컴퓨터)** 에만 열려, 외부 기기에서 접속 불가.
- 비밀번호·키 같은 시크릿은 저장 전 **자동으로 가려집니다**(완벽 보장은 아님).
- 데이터 보관·백업은 **사용자 책임**입니다.

---

## 13. 라이선스 · 저작권 · 상업적 용도 (⚠️ 엄격 기준 — 꼭 읽기)

- **현재 라이선스: Apache License 2.0 © 2026 SoDam AI Studio** (전문은 프로젝트 루트 `LICENSE` 파일).
- **개인·로컬 사용**: 본인 컴퓨터에서 개인 용도로 쓰는 것은 문제없습니다.
- **재배포·수정·상업적 이용**: Apache-2.0 조건(저작권·라이선스 고지 유지, 변경 사항 표시 등)을 지키면 허용됩니다. *단, 이 저장소는 현재 **비공개(private)** 로 운영 중이며 공개 여부는 제작자가 결정합니다.*
- **무보증(AS-IS):** 이 도구는 "있는 그대로" 제공됩니다. **사용 결과·데이터 손실·오작동에 대한 책임은 전적으로 사용자에게** 있으며, 제작자는 어떠한 보증도 하지 않습니다.
- **제3자 약관 별개:** 만약 추후 AI(예: Anthropic Claude) 기능을 켤 경우, **해당 제공사의 이용약관·요금·정책이 별도로 적용**됩니다. O-Brain 라이선스와 무관합니다.
- **상표·이름:** "Claude", "Anthropic" 등은 각 소유사의 상표이며, O-Brain은 그들과 제휴·보증 관계가 아닙니다.
- **내장 라이브러리:** O-Brain은 오픈소스 구성요소(예: better-sqlite3, sqlite-vec, force-graph, @huggingface/transformers 등)를 사용하며, **각 라이브러리의 라이선스가 그대로 적용**됩니다. 배포 시 각 라이선스 고지를 포함해야 합니다.

---

## 14. 솔직한 한계 (현재 v0.1 수준)

- **또렷한 결정 문장**만 잘 기억합니다. 애매한 말은 놓칠 수 있어요.
- **주제 분류는 자동이지만 가끔 어긋납니다**(규칙 기반). **기억 잇기(관계)는 아직 직접** 해야 해요.
- 더 똑똑한 **AI 자동 추출**은 아직 없음(켜려면 별도 API 키·비용 필요 — 현재 미적용).
- **1인·이 PC 전용** 단계입니다(다른 PC 이식·공유는 추가 작업 필요).
- 완성품 보증이 아니라, **직접 써보며 다듬는 중**입니다.

---

*이 문서는 실제 작동을 확인한 값(v0.1, 2026-06-21 기준)으로 작성했습니다. 막히는 부분은 그 화면을 그대로 알려주시면 함께 고칩니다.*
