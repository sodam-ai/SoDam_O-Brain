# O-Brain — 보안 요구사항 (핵심)

> 08 — 보안은 **선택이 아니라 핵심 요구사항**. 기준: **OWASP ASVS Level 1 + Secure by Design**.
> 생성일: 2026-06-20 (v8 — 보안 요구사항 보강)
> 맥락 전제(중요): O-Brain는 **100% 로컬 · 1인 · 무로그인 · 무서버(localhost) · Supabase 미사용**.
> → 일반 웹앱 항목 중 일부는 **그대로는 해당 없음(N/A)**. 억지 적용 대신 실제 구조에 맞게 적용하고, 미해당은 이유와 함께 표시한다.

---

## 0. 보안 원칙

- **Secure by Design**: 기능보다 먼저 안전. 기본값이 가장 안전한 상태(secure default).
- **불신 전제**: 모든 입력(훅 페이로드·검색어·URL 파라미터·폼·API 요청·파일 경로)은 **신뢰하지 않는다**.
- **최소 노출**: 데이터·서버·권한을 필요한 최소로. 로컬 밖으로 절대 새지 않게.

---

## 1. 위협 모델 (O-Brain 특유 — 가장 중요)

| 위협 | 설명 | 핵심 대응 |
|------|------|-----------|
| **로컬 웹서버 노출** | localhost 서버가 같은 네트워크/타 프로세스에 열리면 내 기억 유출 | **127.0.0.1 전용 바인딩** (0.0.0.0 금지) |
| **기억에 시크릿 혼입** | AI 대화에 섞인 키·비번이 그대로 저장 | 저장 전 **시크릿 필터(redaction)** 필수 |
| **기억 내용 XSS** | 대화에서 온 텍스트를 웹에 그대로 렌더 시 스크립트 실행 | 출력 인코딩 + CSP |
| **악성/취약 의존성** | npm 패키지·네이티브 모듈 공급망 위험 | lockfile + `npm audit` + 출처 확인 |
| **경로 조작** | 데이터 경로·코덱스 로그·내보내기 경로 악용 | 경로 정규화 + 허용 루트 제한 |

---

## 2. 인증 · 인가 · 접근 통제 (ASVS V1/V2/V4)

- **로그인 없음**: 내 PC 1인 전용이라 사용자 인증 미도입(의도된 결정).
- 대신 **로컬 접근 통제**로 대체(Must):
  - 서버는 **`127.0.0.1`(루프백)에만 바인딩** — 외부 기기 접근 차단.
  - **CORS**: `Origin`을 localhost(자기 자신)만 허용. 그 외 거부.
  - (Should) **로컬 API 토큰**: 웹↔로컬서버 간 무작위 토큰 1개를 부팅 시 생성해 헤더로 검증(타 프로세스 무단 호출 차단).
- **권한 분리(관리자/일반/비로그인)**: **N/A** — 단일 사용자·단일 권한. (향후 팀/클라우드 도입 시 조건부: §9 참조)

> 수용 기준: `netstat`에서 서버 포트가 **127.0.0.1만 LISTEN**(0.0.0.0 아님). 외부 IP로 접속 시 거부.

---

## 3. 입력값 검증 (불신 전제 · ASVS V5)

검증 대상과 기준:
- **훅 페이로드(JSON)**: 스키마 검증(타입·길이·필수). 파싱 실패 시 안전 폴백.
- **검색어/필터/URL 파라미터**: 길이·타입·허용값 화이트리스트. 과도한 입력 거부.
- **API 요청 바디**: 서버에서 재검증(클라 검증 신뢰 X).
- **파일 경로(데이터 폴더·코덱스 로그·내보내기)**: 정규화 후 **허용 루트 내부만**, `..`·심볼릭 탈출 거부.

> 수용 기준: 잘못된 타입·초과 길이·경로 탈출 입력에 대해 **저장/실행 없이 안전 거부 + 친절 메시지**.

---

## 4. 인젝션 방지 (ASVS V5) — 요구사항 + 수용 기준

| 공격 | 요구사항 | 수용 기준 |
|------|---------|-----------|
| **SQL Injection** | better-sqlite3 **prepared statement(바인딩 파라미터)만** 사용. FTS5/vec 쿼리도 파라미터화 | 문자열 연결 쿼리 **0건**(코드 grep) |
| **XSS** | ~~기억 내용은 React 기본 이스케이프~~ → **[확인됨, 2026-08-03 갱신] React 미사용 확정** — 화면에 뿌리는 모든 기억 `content`는 직접 구현한 `esc()`(`&<>"` 4종 치환) 또는 이를 내부 호출하는 `hl()`(검색어 하이라이트)를 반드시 거침(`web/index.html`). 그래프 툴팁은 `textContent`(innerHTML 금지). **CSP `default-src 'self'`** | 외부 스크립트 0. ~~`dangerouslySetInnerHTML` 0~~(React 없어 해당 API 자체가 없음) → **이스케이프 없이 `innerHTML`에 직접 꽂히는 지점 0건**(코드 grep 기준, 08_SECURITY 갱신 시점 재확인) |
| **경로 조작** | 모든 파일 경로 정규화 + 허용 루트 검사 | `..` 포함 경로 거부 테스트 통과 |
| **명령어 주입** | 셸에 문자열 보간 금지. 외부 실행은 인자 배열 방식 | `exec(`문자열 보간`)` 0건 |
| **위험한 파일 업로드** | **N/A** — O-Brain는 파일 업로드 기능 없음. (향후 자료 첨부 도입 시: 확장자·MIME·크기 화이트리스트 + 실행 차단) | 업로드 엔드포인트 부재 확인 |

---

## 5. 비밀정보 관리 (ASVS V6/V7) — 코드/문서/로그/Git 비노출

- **저장 단계 시크릿 필터(필수)**: 기억 `content` 저장 전 API키·토큰·비번·`.env` 값·사설키 블록을 `[REDACTED]` 치환(05 §2와 연결).
- **키 보관**: ~~`ANTHROPIC_API_KEY` 등은 `.env.local`에만~~ → [확인됨] **미구현·미사용**(호스트 LLM 재활용으로 원안 자체 폐기, 코드 전수 검색 0건 — 04_PROJECT_SPEC.md §기술스택 참조). 코드·그래프·UI·로그·에러메시지에 시크릿 노출 금지 원칙 자체는 유지.
- **Git 추적 제외**: `.gitignore`에 `.env*`, `data/`, `*.sqlite`, 백업 포함(이미 반영). 커밋 전 **시크릿 스캔**(gitleaks/grep) 권장.
- **로그**: 토큰·키·기억 원문을 로그에 남기지 않음. 디버그 로그에도 마스킹.
- 대상 시크릿 목록: API 키, 토큰, 비밀번호, `.env`, 인증서, 개인 키, DB 접속정보, OAuth Secret, 서비스 계정 키.

> 수용 기준: 저장소·로그에 위 시크릿 **0건**(스캔 통과). 시크릿 섞인 대화 테스트 시 `content`에 평문 없음.

---

## 6. 세션 · 토큰 관리 (ASVS V3)

- **세션/JWT/OAuth/비밀번호 재설정 토큰/초대 코드**: **대부분 N/A**(무로그인·무서버 인증).
- 도입 대상은 **로컬 API 토큰 · 훅 nonce(선택, Should)**:
  - **안전한 생성**: `crypto.randomBytes`(CSPRNG, 추측 불가). `Math.random` 금지.
  - **저장**: 메모리 또는 `.env.local`(권한 제한). Git·로그 금지.
  - **만료**: 토큰은 앱 실행 단위로 회전(재시작 시 재발급). 훅 nonce는 1회용.

---

## 7. 보안 설정 (ASVS V14)

- **CORS**: localhost 자기 출처만 허용, credentials 최소.
- **쿠키**: 쿠키 미사용이 기본. 부득이 사용 시 `HttpOnly`·`SameSite=Strict`·`Secure`(로컬은 localhost 한정).
- **보안 헤더**: `Content-Security-Policy: default-src 'self'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, `X-Frame-Options: DENY`.
- **환경 변수**: 설정은 `.env.local`. 코드 하드코딩 금지.
- **디버그/운영 분리**: 운영(기본)에서 **스택트레이스·상세 에러 비노출**(사용자엔 친절 메시지, 상세는 로컬 로그만). dev 모드에서만 상세.

---

## 8. 의존성 보안 (공급망)

- **lockfile 커밋**(package-lock.json) — 버전 고정.
- **`npm audit`** 정기 실행, **high/critical 0** 유지(수용 기준).
- 네이티브 모듈(better-sqlite3)·로컬 임베딩 모델은 **공식 출처**만. 모델 파일 무결성 확인.
- 의존성 추가 시 필요 최소만(공격면 축소).

---

## 9. DB · 스토리지 권한 / Supabase

- **로컬 SQLite**: `data/` 폴더·DB 파일은 **현재 사용자 권한**으로만 접근(OS 파일 권한). 공유 폴더·동기화 폴더(클라우드 드라이브) 저장 지양(의도치 않은 유출 방지).
- **백업 파일**도 동일 보호(`.gitignore` 포함).
- **Supabase RLS / anon key·service role key / Storage 공개범위**: **N/A — O-Brain는 Supabase·외부 DB를 쓰지 않음**(100% 로컬 SQLite).
  - **조건부(향후)**: 만약 Phase 3에서 선택적 클라우드 동기화를 도입한다면 — RLS 필수, **service_role 키는 절대 클라이언트 노출 금지(서버에서만)**, anon 키만 클라이언트, Storage는 비공개 기본. (현재 범위 밖)

---

## 10. 제약 (기존 로직 보존)

- 보안 보강은 **기존 기능·데이터 흐름·저장(SQLite)·추출 로직·MCP·검색을 불필요하게 변경하지 않는다.**
- 인증 없음/로컬 전용/결제 없음 전제 유지. (보안을 이유로 무로그인 정책을 뒤집지 않음 — 대신 §2 로컬 접근통제로 보완)

---

## 11. 보안 우선순위 (MoSCoW)

**Must (P1 필수)**
- 127.0.0.1 전용 바인딩 + CORS 로컬 제한
- 시크릿 필터(저장 전) + .env.local + .gitignore + 로그 비노출
- SQLi 방지(파라미터화) · XSS 방지(이스케이프+CSP) · 경로조작 방지
- 입력 검증(불신 전제) · 운영 에러 비노출 · lockfile

**Should (P1~P2)**
- 로컬 API 토큰(웹↔서버) · 보안 헤더 일습 · `npm audit` high/critical 0
- 훅 nonce(1회용) · 시크릿 커밋 스캔(gitleaks)

**Could (P2~P3)**
- 데이터 폴더 선택적 암호화(at-rest) · 무결성 검증 · (도입 시) 클라우드 동기화 RLS

---

## 12. [확정 / 결정]

- 보안 = **핵심 요구사항**(선택 아님). 기준 = **OWASP ASVS L1 + Secure by Design**.
- 무로그인은 유지하되, **로컬 접근통제(루프백 바인딩·CORS·선택 토큰)**로 대체 보완.
- Supabase/JWT/OAuth/역할분리/파일업로드 = **현재 N/A**, 향후 기능 도입 시 조건부 적용(위 명시).
