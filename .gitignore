# 의존성 / 런타임 로그
node_modules/
*.log

# 데이터·DB (절대 커밋 금지 — 개인 기억/백업)
data/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# 비밀정보 (절대 커밋 금지)
.env
.env.local
.env.*.local

# 빌드 / 캐시
dist/
build/
.vite/

# 플러그인 자동생성 캐시 / 임시
.omc/
.plugin-config/
*.todos-report.md
.complexity-log.md

# 다른 SoDam 도구(sodam-reverse)가 이 작업폴더를 cwd로 쓰며 남기는 안전로그 — O-Brain과 무관, 실수 커밋 방지
# 위치 무관(저장소 최상위에도 별도로 생김, 2026-08-03 실측 확인) 매칭
.sodam-re/

# OS / 에디터
.DS_Store
Thumbs.db

# 사용자 지정 — 파일명 자체가 "푸시 금지" 지시. 절대 추적/푸시 안 함.
이 문서는 반드시 깃허브 저장소에 푸시 하지 않기.md

# ai-pair-programming 자동 세션 리포트
.pair-programming-session.md

# 로컬 전용 Claude Code 프로젝트 설정 (절대경로 포함 — 다른 PC에서 동작 안 함)
.claude/

# 내부 계획·감사 문서 (저장소 불필요)
PLAN_PRD_AUDIT.md
